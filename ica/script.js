function enableScroll() {
    var heroSection = document.getElementById('hero');
    heroSection.style.display = 'none'; // Menghilangkan section hero
    document.body.style.overflow = 'auto'; // Mengizinkan scroll pada body
}